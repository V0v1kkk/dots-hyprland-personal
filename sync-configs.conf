# Configuration file for end4_dotfiles sync scripts
# List of config directories/files to sync between local ~/.config and fork
# Add paths relative to .config directory (one per line)
# Lines starting with # are comments

hypr
quickshell
fuzzel
wlogout
